# 代码审查：开发规范问题清单

> 审查项目：bitm-code-nav（已执行 5.1 组件拆分）
> 审查日期：2026-04-29

---

## 一、运行时隐患

### 1.1 主题 class 初始状态缺失 ✅ 已修复

**修复内容（2026-04-29）：** `watch` 添加 `{ immediate: true }`，同时用 `import.meta.client` 守卫 `localStorage` 调用以避免 SSR 崩溃。

### 1.2 搜索历史无上限增长 ✅ 已修复

**文件：** `app/composables/useSearch.ts:37-38`

**修复内容（2026-04-29）：** 改为去重 + 限制最近 50 条：

```diff
- searchStringList.push(searchString)
+ const idx = searchStringList.indexOf(searchString)
+ if (idx !== -1) searchStringList.splice(idx, 1)
+ searchStringList.unshift(searchString)
+ if (searchStringList.length > 50) searchStringList.pop()
  window.localStorage.setItem('searchStringList', JSON.stringify(searchStringList))
```

- 重复字符串移到最前，避免重复存储
- 超过 50 条移除最早记录
- `watch` 遍历性能保持在可控范围

---

## 二、代码质量问题

### 2.1 残留 console.log ✅ 已修复

**修复内容（2026-04-29）：** 注释掉 `console.log` 语句。

### 2.2 localStorage 读取逻辑重复 ✅ 已修复

**修复内容（2026-04-29）：** 抽取 `readSearchHistory(): string[]` 工具函数，`handleSearch` 和 `watch` 回调均调用它。

### 2.3 冗余的显式类型标注 ✅ 已修复

**修复内容（2026-04-29）：** 简化 `index.vue` 中的类型标注：

```typescript
const websiteData = ref(websiteConfigData)
const searchData = ref(searchConfigData)
const mainBoxRef = ref<HTMLElement | null>(null)
const contextBoxRef = ref<HTMLElement | null>(null)
```

同时移除了不再需要的 `Category` 和 `SearchModel` import。

### 2.4 引号风格不一致 ✅ 已修复

**修复内容（2026-04-29）：** `index.vue` 所有 import 统一为单引号，同时移除了不再需要的 `Category` 和 `SearchModel` 导入。

---

## 三、数据与类型

### 3.1 Category 接口与数据不匹配 ✅ 已修复

**文件：** `app/model/Category.ts:3-7`

```typescript
export interface Category {
    category: string
    iconSvg: string
    children: WebsiteModel[]
}
```

**文件：** `app/dao/WebSiteData.ts` 每个分类对象：

```typescript
{
    "category": "站长推荐",
    "isSelected": false,      // ← 接口未定义，代码未使用
    "isMouseenter": false,    // ← 接口未定义，代码未使用
    "iconSvg": "svg/nav/recommend.svg",
    "children": [...]
}
```

**修复内容（2026-04-29）：** 从 `app/data/WebSiteData.ts` 所有分类对象中删除了 `isSelected` 和 `isMouseenter` 字段。

### 3.2 类型命名冗余 ✅ 已修复

**修复内容（2026-04-29）：**
- `WebsiteModel.ts` → `Website.ts`，接口 `WebsiteModel` → `Website`
- `SearchModel.ts` → `SearchConfig.ts`，接口 `SearchModel` → `SearchConfig`
- 所有引用已更新

### 3.3 默认搜索引擎 logo 路径不一致 ✅ 已修复

**修复内容（2026-04-29）：** 默认值统一为 `svg/search/search-bing.svg`，与数据文件一致。

---

## 四、架构

### 4.1 `dao/` 目录命名不当 ✅ 已修复

**修复内容（2026-04-29）：** `app/dao/` → `app/data/`，更新了 `index.vue` 中的 import 路径。

### 4.2 `app.vue` 未被充分利用 ✅ 已修复

**修复内容（2026-04-29）：** header、sidebar、toolbar 已从 `index.vue` 提升到 `app.vue` 根布局。`index.vue` 当前只含搜索栏、分类区块和页脚。

---

## 五、构建与部署

### 5.1 `re-build` 脚本无错误处理 ✅ 已修复

**修复内容（2026-04-29）：** 该脚本已无用，直接删除。

---

## 六、修复优先级

| 优先级 | 问题 | 状态 |
|--------|------|------|
| 🔴 紧急 | 1.1 主题 class 初始状态缺失 | ✅ 已修复（2026-04-29） |
| 🟡 高 | 2.1 残留 console.log | ✅ 已修复（2026-04-29） |
| 🟡 高 | 2.2 localStorage 读取逻辑重复 | ✅ 已修复（2026-04-29） |
| 🟡 高 | 3.1 冗余数据字段 / 接口不匹配 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 1.2 搜索历史无上限增长 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 2.3 冗余显式类型标注 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 2.4 引号风格不一致 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 3.2 类型命名冗余 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 3.3 默认 logo 路径不一致 | ✅ 已修复（2026-04-29） |
| 🟢 中 | 4.1 目录命名 | ✅ 已修复（2026-04-29） |
| ⚪ 低 | 4.2 app.vue 布局提升 | ✅ 已修复（2026-04-29） |
| ⚪ 低 | 5.1 re-build 错误处理 | ✅ 已修复（2026-04-29） |
